Rule: react-doctor/js-set-map-lookups
Severity: warning
Category: Performance
Count: 1

This scales poorly because `array.includes()` inside a loop scans the whole list every time. Use a Set for constant-time lookups.

Suggestion: Use a `Set` or `Map` when you check for the same items over and over. `Array.includes`/`find` scans the whole list each time

Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/js-set-map-lookups

Files:
  src/chess/rules/CheckDetector.js:16
